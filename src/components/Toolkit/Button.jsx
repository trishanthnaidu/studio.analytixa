import React from 'react';
import { Button as MUI_Button } from '@material-ui/core';

export const Button =
      props =>
            <MUI_Button
                  color="primary"
                  {...props}
            /> 
